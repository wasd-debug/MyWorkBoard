# Runtime-only image for deployment after a local Maven build.
FROM eclipse-temurin:17-jre
WORKDIR /app
COPY backend/target/salary-tracker-backend.jar app.jar
ENV PORT=8080
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "/app/app.jar"]
